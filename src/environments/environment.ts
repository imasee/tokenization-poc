export const environment = {
  production: false,
  /** Mock Branding API origin — intercepted by installMockBrandEndpoint(), never hits the network. */
  brandEngineUrl: 'https://mock.brand-engine.local',
  brandIdentifier: 'kt',
};
